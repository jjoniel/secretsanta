# Database
# For development, SQLite is fine. For production, use PostgreSQL
DATABASE_URL=sqlite:///./santa.db
# Example PostgreSQL: DATABASE_URL=postgresql://username:password@localhost/santa

# Security
# Generate a random secret key for JWT tokens (used for user authentication)
# You can generate one with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production

# CORS - allowed frontend origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# For production: CORS_ORIGINS=https://yourdomain.com

# Trusted hosts (for production)
TRUSTED_HOSTS=localhost,127.0.0.1
# For production: TRUSTED_HOSTS=yourdomain.com,www.yourdomain.com

# Gmail API Configuration
# This is YOUR email address that will SEND the Secret Santa emails
# It's the "from" address - participants will receive emails FROM this address
SANTA_EMAIL=your-email@gmail.com

# Gmail API files (these are created automatically after OAuth setup)
# token.json is created after first OAuth login
# credentials.json you download from Google Cloud Console
GMAIL_TOKEN_FILE=token.json
GMAIL_CREDENTIALS_FILE=credentials.json

